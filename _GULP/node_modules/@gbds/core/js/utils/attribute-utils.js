export function setAttribute(element, attributeName, state) {
    element.setAttribute((attributeName), state);
}
export function getAttribute(element, attributeName) {
    return element.getAttribute(attributeName);
}
