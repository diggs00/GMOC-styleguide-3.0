export function fireAnalyticsEvent(namespace, componentName, eventName, type, data) {
    const name = `${namespace}-${componentName}-${eventName}`;
    const detail = { data, type };
    const customEvent = new CustomEvent(name, { detail });
    document.dispatchEvent(customEvent);
}
