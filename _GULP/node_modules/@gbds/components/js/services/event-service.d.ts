export interface Subscription {
    name: string;
    subscriptionNumber: number;
}
export declare class EventService {
    private static subscriptionList;
    static subscribe<T>(name: string, callback: Function, context: T): Subscription;
    static publish(name: string, ...args: any[]): void;
    static unsubscribe(subscription: Subscription): void;
}
