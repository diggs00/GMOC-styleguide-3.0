import { TypeString } from '../models/type-string';
export declare function component(tag: string): (Ctor: CustomElementConstructor) => void;
export interface PropAttributeMetadata {
    changedFn: string;
    type: TypeString;
}
export declare abstract class GbdsElement extends HTMLElement {
    constructor();
    private static observedProps;
    private static get observedAttributes();
    protected attributeChangedCallback(name: string, oldVal: string | null, newVal: string | null): void;
}
